# Installation dependencies have one source of truth: pyproject.toml.
#
# Runtime:     python -m pip install .
# Development: python -m pip install -e ".[dev]"
# Wheel:       python -m build --wheel
#
# This file intentionally contains no second, drifting dependency graph.
