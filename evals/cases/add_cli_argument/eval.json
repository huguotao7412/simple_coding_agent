{
  "name": "add_cli_argument",
  "prompt": "Add a --shout CLI flag that prints the greeting in uppercase. Preserve the existing default behavior.",
  "verification_commands": [
    "python -m pytest -q"
  ],
  "expected_files": [
    "greeter.py",
    "test_greeter.py"
  ],
  "timeout_seconds": 60
}
