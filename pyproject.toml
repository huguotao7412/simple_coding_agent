[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "simple-coding-agent"
version = "0.1.0"
description = "CLI-first transparent coding agent runtime"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.12"
authors = [{ name = "huguotao7412" }]
keywords = ["coding-agent", "cli", "llm", "mcp"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development",
]
dependencies = [
    "httpx>=0.28",
    # MCP 2.x changed its public Python object model. Keep production installs
    # on the compatibility line covered by the wheel/pipx contract tests.
    "mcp>=1.28,<2",
    "rich>=13",
    "python-dotenv>=1.0",
    "e2b>=2.11,<3",
    "langgraph>=1.0,<2",
    "langgraph-checkpoint-sqlite>=3.0,<4",
]

[project.scripts]
sca = "cli.main:main"
sca-eval = "evals.cli:main"
sca-harbor-agent = "evals.harbor_entrypoint:main"
sca-web = "web.cli:main"

[project.urls]
Homepage = "https://github.com/huguotao7412/simple_coding_agent"
Repository = "https://github.com/huguotao7412/simple_coding_agent"
Issues = "https://github.com/huguotao7412/simple_coding_agent/issues"

[project.optional-dependencies]
guardrails = [
    "openai-guardrails>=0.2.1,<0.3",
]
dev = [
    "build>=1.2",
    "mypy>=1.10",
    "pytest>=8",
    "pytest-asyncio>=0.25",
]
web = [
    "streamlit>=1.40",
]
benchmark = [
    "harbor>=0.18,<0.19",
    # Newer LiteLLM releases currently fall back to a Rust source build on Windows.
    "litellm==1.83.14",
    "setuptools>=75",
    "wheel",
]

[tool.setuptools.packages.find]
include = ["cli*", "core*", "evals*", "web*"]

# The pinned Node dependency graph is installed explicitly by `sca mcp
# install`; it is data, not an implicit Python post-install hook.
[tool.setuptools.data-files]
"share/simple-coding-agent/mcp" = ["package.json", "package-lock.json"]

[tool.mypy]
strict = true
python_version = "3.12"
follow_imports = "silent"
exclude = [".venv", ".worktrees", "__pycache__"]
ignore_missing_imports = true
files = [
    "core/application",
    "core/domain",
    "core/ports",
    "core/adapters",
    "core/a2a_lite",
    "core/orchestration",
    "core/execution",
    "core/config.py",
    "core/model_names.py",
    "core/sandbox",
    "core/security",
    "core/actors/contracts.py",
    "core/policy.py",
    "core/paths.py",
    "core/lifecycle.py",
    "core/events.py",
    "core/runs/models.py",
    "core/runs/store.py",
    "core/runs/sqlite_store.py",
    "core/runs/context.py",
    "core/runs/task_state.py",
    "core/runtime/engine.py",
    "core/runtime/model_loop.py",
    "core/runtime/tool_cycle.py",
    "core/runtime/message_payload.py",
    "core/runtime/loop_control.py",
    "core/runtime/tool_calls.py",
    "core/planner.py",
    "core/actors/agent.py",
    "core/actors/worktree.py",
    "core/git_utils.py",
    "core/verification",
    "core/mcp/client.py",
    "core/mcp/managed_runtime.py",
    "core/tools/update_state.py",
    "core/tools/gateway.py",
    "core/tools/catalog.py",
    "core/tools/models.py",
    "core/tools/delegate.py",
    "core/tools/apply_patch.py",
    "core/tools/file_ops.py",
    "core/tools/read.py",
    "core/tools/sandbox_run.py",
    "cli/report.py",
    "cli/runs.py",
    "cli/main.py",
    "evals/run_evals.py",
    "evals/security_eval.py",
    "evals/harbor_entrypoint.py",
    "evals/harbor_runner.py",
    "evals/harbor_support.py",
    "evals/durability.py",
]

[[tool.mypy.overrides]]
module = ["deepseek_tokenizer", "streamlit", "dotenv", "rich"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = [
    ".git",
    ".venv",
    ".worktrees",
    "node_modules",
    "evals/fixtures",
    "tmp",
]
